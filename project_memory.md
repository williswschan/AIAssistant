# Tech Cafe AI Helpdesk ??Project Memory

## Purpose
Standalone POC for ??Talk to Tech Cafe AI?? browser voice Help Desk using realtime speech-to-speech (S2S), later intended to sit beside the existing Tech Cafe Portal appointment flow.

## Git remote
- Canonical repo: https://github.com/williswschan/AIAssistant.git (`origin` / `main`). VM working copy: `/opt/techcafe`.

## Key architectural decisions
- Transport: browser WebRTC to Azure OpenAI Realtime.
- Provider: **Azure OpenAI / Microsoft Foundry** (`foundry-techcafeaiassistant`, project `proj-techcafeaiassistant`) ??not OpenAI Platform and not chatgpt.com Advanced Voice.
- Auth pattern: Flask proxies session setup ??mint ephemeral `client_secrets` with permanent `api-key`, negotiate SDP to `/openai/v1/realtime/calls`, return **SDP answer only** to browser.
- Default model deployment: **`gpt-realtime-2.1`** (set in `.env` as `AZURE_OPENAI_REALTIME_DEPLOYMENT`; also available in Foundry: `gpt-realtime`, `gpt-realtime-mini`). Voice agent is Realtime 2.1, not the older `gpt-realtime` alone.
- Voice: `marin` (set via `AZURE_OPENAI_VOICE`). On voice switch: update `.env`, restart Flask, and ensure `/token` returns `voice` while browser `session.update` re-asserts `audio.output.voice` (required ??VAD-only update can leave timbre sounding wrong). Client pins `audio.output.voice` on every `response.create`, greets only after `session.updated` confirms voice, and prompt forbids mid-call voice/announcer-style switches (booking lines).
- Dual-model mode: voice = `gpt-realtime-2.1`; diagnostics tool `consult_helpdesk_expert` ??Flask ??Azure **Responses API** (`/openai/v1/responses`) `gpt-6-astra`. Chat Completions (`/chat/completions`) returns 404 "not a chat model" for Astra on this resource. Browser relays Realtime function calls; permanent key stays on server. Tool args include optional **`known_facts`** plus richer `already_tried` so Brain does not re-ask settled call facts (anti-double-ask). Post-consult voice coaching also strips re-asks.
- Prompt cache: **gpt-6-astra** uses explicit `prompt_cache_key` + `prompt_cache_retention` (24h) + startup warm-up. **Realtime voice** uses automatic in-memory caching when Azure supports it; app sets `truncation.retention_ratio=0.8` and logs `cached_tokens` from `response.done` when present.
- Post-consult speech: competing Realtime hold + tool reply caused mid-sentence cutoffs. Fix retained: cancel/clear before specialist reply, one `response.create` with speak_to_caller coaching. **Hold audio (option B):** continue-to-hold uses **Realtime/marin** again (local TTS only as fallback) so the caller hears one voice.
- Brain prompt caching: diagnose calls send a stable system prefix (`techcafe_prompt.txt` wrapper) with `prompt_cache_key` + `prompt_cache_retention=24h` (gpt-6-astra extended cache). Startup warm-up thread primes the cache. Logs `cached_tokens` on each diagnose.
- Expert-wait UX: voice prompt requires an explicit ??please hold / checking with diagnostic specialist??before the tool; while Brain runs, browser speaks continue-to-hold every **5s** (Realtime/marin; local SpeechSynthesis fallback; **English / Japanese / Mandarin / Cantonese** lines). Hold must stop the moment Brain returns. **Race (call-20260912-211033-1e05):** Brain returned in ~7s at 21:11:49; a late continue-to-hold `response.create` (English) was claimed as the specialist reply at 21:11:54, then coached Mandarin advice was suppressed ??~30s silence until barge-in. Fix: track `holdOutboundPending`, drain/cancel late holds before arming, never claim hold as `diagnoseOwnResponseId`, retry if own transcript still looks like a hold line.
- Post-Brain delivery: mute during kill (~1.2s) then drain holds then arm; first `response.created` after arm is the coached reply; unmute only on that id??s audio. Removed the 450ms ??too soon??cancel (it killed our own create ??silence in `call-20260912-164950-d12d`). Do not `response.cancel` extras while own is speaking.
- Auto hang-up: end_call must be paired with a real farewell. Client also schedules hangup when closing farewell speech is heard without the tool (call-20260913-132902-a0d2: Goodbye spoken, no end_call, call stayed open). Procedural lines still force a dedicated goodbye; barge-in during hangup does not abort ending.
- Turn detection: `server_vad` with `create_response` + `interrupt_response`. Session is created with both **false** so the greeting cannot be server-cancelled; after greeting audio ends the browser `session.update`s them back on (from `AZURE_INTERRUPT_RESPONSE` / token config) and clears the input buffer.
- Opening greeting: fixed `Hello, Tech Cafe IT Help Desk.` + one of 5 similar second lines chosen randomly per call in `app.js` (avoids immediate repeat via sessionStorage).
- Agent behavior: loaded from `techcafe_prompt.txt` into session instructions (not hard-coded in JS).
- UI: matches Tech Cafe Booking Portal look ? red gradient page, white rounded shell, red header/footer. Start button uses artwork `static/ai-assistant-btn.png` (mic + **AI Assistant** + “Talk to IT. Get instant help.” + rainbow NEW; white surround stripped to alpha). **End Call** remains the CSS red gradient pill. Mic opens only after Start and is released on hang-up. Connecting ring: North American PSTN **ringback** (440+480 Hz, 2s on / 4s off); stops just before Hello. Greeting protected by **mic hold** + `turn_detection: null` until playback ends; then VAD/interrupt restored. `enableLocalPlayback` re-calls `remoteAudio.play()`.
- VM-only extras: Start click kickstarter (`window.__tcKick`) remains on the VM copy of `app.js` / HTML; local uses normal `app.js` click binding.
- Control WSS (`/ws/control`): client ping every **8s** + auto-reconnect on close. Console `Invalid frame header` usually means reverse proxy idle-closed the socket; if reconnect still drops mid-call, raise proxy WebSocket idle timeout on `ai.mymsngroup.com`.

## Important assumptions
- **OS timezone:** VM set to `Asia/Hong_Kong` (HKT, GMT+8) on 2026-09-16 (was UTC). Transcript/ticket `datetime.now()` stamps follow local time after service restart.
- Azure Realtime deployment exists and region supports Realtime.
- Local Windows + Chrome/Edge for mic/WebRTC.
- ServiceNow and Tech Cafe booking remain simulated in the prompt.

## Non-obvious constraints
- ChatGPT Plus / Project instructions cannot drive a custom in-page voice button.
- Catalog model IDs ??usable until deployed in the Foundry project.
- Permanent keys must never ship to the browser; `.env` is gitignored.
- API key was shared in chat during setup ??rotate when practical.

## Useful conclusions
- Project zip backups go in `MISC/` as `TechCafe-backup-yyyyMMdd-HHmmss-vNN.zip` (excludes `MISC/`, `.venv/`, `__pycache__/`, `logs/transcripts/`, `certs/`). Latest: **v18** (`TechCafe-backup-20260915-122859-v18.zip`) ? OS-default mic/speaker for ring+AI (setSinkId default), gpt-6-astra Brain, foundry-techcafeaiassistant endpoints, greeting variants, farewell hangup. **Includes `.env` (secrets)** ? do not share publicly.
- `app.py` function/route flow chart (Mermaid): `MISC/app-py-flow.md`.
- Hosting preference: public URL + Python + cheap; prefer not maintaining Docker images. Current ACI. Alternatives: small Azure VM (public IP, no custom VNet) or App Service code deploy. Azure OpenAI `api-key` cannot create VMs ? need subscription + `az login` or service principal.
- Azure VM (2026-09-13): `vm-teachcafeaiassistant` in `rg-default` / eastasia, Standard_B1ms, AlmaLinux 9.8, Public IP 52.184.99.233. Public DNS: **`techcafeaiassistant.eastasia.cloudapp.azure.com`** (updated from `teahcafeaiassistant...` / earlier `vmteachcafeaiassistant...`). App at `/opt/techcafe`, systemd `techcafe.service`, SSH key `~/.ssh/id_rsa_vm_teachcafeaiassistant`. TLS self-signed `dev-cert.pem` with SANs for IP + FQDNs. **Deallocated 2026-09-13** to save compute cost (disk still bills; start with `az vm start -g rg-default -n vm-teachcafeaiassistant`).
- Button UI synced local ? VM (2026-09-15): **AI Assistant** + NEW ribbon; **End Call** same size/gradient, no ribbon. VM still has `window.__tcKick` click workaround; local uses normal listeners.
- App entrypoint: **gunicorn + gevent** (`wsgi:app`, `gunicorn.conf.py`) on `0.0.0.0:443` HTTPS — replaces Werkzeug `app.run()` which wedged with `CLOSE-WAIT` under public scan + long-lived `/ws/control`. systemd `techcafe.service` (`Restart=always`). `python app.py` execs the same gunicorn config. Workers=1 (POC, WebSocket-friendly); `timeout=0`; `max_requests=1000` recycles leaked sockets. Local URL https://127.0.0.1:443
- iPhone note: plain `http://LAN-IP` leaves `navigator.mediaDevices` undefined (insecure context). Use `https://LAN-IP:5000`, accept self-signed cert warning; mic opens on button tap (user gesture).
- Transcript logging: `TRANSCRIPT_LOGGING` in `.env` (default true for POC troubleshooting; set false for production). Files go to `logs/transcripts/call-YYYYMMDD-HHMMSS-xxxx.txt`. **Transcript file is created server-side on `GET /token`** (returns `transcript_id`); browser appends speech/tool lines. Assistant speech + tools logged when enabled; user ASR text requires optional `AZURE_OPENAI_TRANSCRIPTION_DEPLOYMENT`. **User ASR disabled 2026-09-16** (empty `AZURE_OPENAI_TRANSCRIPTION_DEPLOYMENT`; previously worked with `gpt-live-transcribe`). Always-on breadcrumbs also in `logs/client-events.log` via `GET /api/client-log`.
- **Call ticket CSVs (2026-09-16):** After each call, server writes `logs/tickets/ticket-YYYYMMDD-HHMMSS-xxxx.csv` with `caller_name`, `ticket_creation_time`, `started_at`, `ended_at`, `problem_summary`, `solution_summary`, `resolved`. Times are server-filled (`started_at` on `/token` call session; `ended_at` / `ticket_creation_time` on finalize). Outcome fields come from extended `end_call` tool args; client also drafts from `consult_helpdesk_expert` + Tech Cafe booking cues. If the model only fills `reason`, server **backfills from the call transcript**. `resolved=false` for ServiceNow / Tech Cafe escalation; `true` only if fixed on-call. APIs: `POST /api/ticket`, finalize also on `POST /api/transcript/end`.
- **TEMP ServiceNow ticket portal (2026-09-16):** Isolated process under `misc/servicenow/` (gitignored; do not import into `app.py`). Lists `logs/tickets/*.csv` at **`http://techcafeaiassistant.eastasia.cloudapp.azure.com/`** (port **80**, HTTP, site root — no `/servicenow` required). Start: `sudo .venv/bin/python misc/servicenow/portal.py`. Delete `misc/servicenow/` when going live. Local firewall: `http` service. Azure NSG may also need inbound **80**. Main voice app remains on **443**.

- Brain consult (LAN Edge client `192.168.1.201`): mid-call `fetch` POST/GET to Flask over self-signed HTTPS often failed (`Failed to fetch` / connection reset). **Fix verified on Microsoft Edge** call `010347-2819`: persistent **`WSS /ws/control`** for `expert_start` + `expert_status` (`via=ws`, `path=remote`). GET/POST remain fallbacks. Breadcrumbs: `logs/client-events.log`.
- Endpoints:
  - Azure OpenAI: `https://foundry-techcafeaiassistant.openai.azure.com/openai/v1`
  - Foundry project: `https://foundry-techcafeaiassistant.services.ai.azure.com/api/projects/proj-techcafeaiassistant`
  - **Source of truth:** live `/opt/techcafe/.env` (endpoints/deployments may have changed from older mymsngroup / prior Foundry setups; do not trust stale README/memory over `.env`).
  - Transcript APIs: `POST /api/transcript/start|append|end`
- Voice-path: Azure-documented browser WebRTC (`GET /token` ??browser POSTs SDP to Azure with ephemeral key). Data channel `oai-events`, `server_vad`, level meter; **mic + speaker dropdowns** under AI Assistant (persist in localStorage; **System default** follows Windows *currently selected for calls* via the `communications` role when present, else multimedia `default` ? not a pinned hardware id). Speaker list uses `setSinkId` when supported (hidden if not).
- Desktop Bluetooth / OS default I/O: with System default, mic prefers Windows **Default Communications Device**, then falls back to OS default. Playback uses `communications` then `default` so laptop Speakers can remain the multimedia default while the headset currently selected for calls is used. Explicit dropdown picks still force that deviceId. Ringtone + AI share one sink. HFP Bluetooth call audio is inherently narrowband vs A2DP/music.
- Call quality / barge-in: on `speech_started` cancel active response, clear output audio, truncate heard audio, mark response IDs cancelled so stale playback/tool `response.create` cannot resume. Interrupted turns are CANCELLED not paused. VAD silence default **900ms**; **VAD threshold 0.9** (was 0.8; higher = less noise-sensitive). Do not mute mic track (Bluetooth-safe). **Do not set threshold to 1.0** — Azure `server_vad` then never detects speech (call-20260916-074755-9498: Hello only, no USER speech_started, no reply); 1.0 → 0.8 → 0.9 on 2026-09-16.
- Prompt: full **VOICE INTERACTION QUALITY** block (OpenAI-style spoken-conversation rules) plus Tech Cafe triage/tools; stop phrases include Got it / I know / ??? / ?????? never finish interrupted instructions; respond only to newest user intent.
- AD lockout routing: clear AD/account lockout ??Path B ServiceNow immediately; ban fake ??checking/unlocking on the backend / please wait??stalls. Prompt + GPT-5.5 diagnose coaching updated.
- Long silence during triage was mainly GPT-5.5 `/api/diagnose` latency (~45??5s historically) after the voice agent said it would ??check?? mitigated with prompt caching/warm-up + explicit hold / continue-to-hold speech (not by removing the expert tool).
- One-reply-per-user-turn guard: Realtime sometimes emitted a short stall line then a second full reply with no caller speech (`call-20260912-205224-3fa7`); client cancels extras (`suppress_double_reply`); prompt forbids split stall+follow-up.
- Language: match the caller's **latest spoken CONTENT** language/dialect (English / Japanese / Mandarin / Cantonese). Detect from sentence grammar/particles ? **ignore English or romanized names** (Willis Chan + Mandarin issue => Mandarin). Do not lock English from greeting/name; IT brand words inside Chinese/Japanese do not count as English. HK bias: do not default all Chinese to Cantonese.
- Barge-in accept UX: if caller interrupts a Tech Cafe/booking offer with yes/okay (`call-20260913-114515-c241`, `call-20260913-120311-cffc`), advance to offer/confirm the simulated slot ??do not restate the same recommendation. Client coaches this on speech_stopped when `bookingOfferStage` is techcafe_pitch/slot_offer (kills auto-reply, one pinned voice reply).
- **Ring-but-no-Hello (2026-09-16):** Greeting holds mic (`track.enabled=false`) so Azure cannot barge into Hello. Outbound stats treated silent+0-byte uplink as `dead_uplink` and `after_remote_audio` recovered when meter &lt;2% — false positives that looped `getUserMedia`/`replaceTrack` while ICE was still `connecting` (console: `call-20260916-004506-227e`). Fix: skip mic recover for non-dead-track reasons while `greetingMicHeld || !callAudioReady`; do not count intentional silence toward dead_uplink; after remote audio during greeting only `ensureLiveMicOnSender`.
- **Name spelling vs ticket (call-20260916-075144-c395):** Caller spelled C-A-L-V-I-N but voice still said Kelvin and `end_call` wrote `Kelvin Chen`. Prompt now treats letter spelling as authoritative; tool `caller_name` descriptions updated; client parses "spelled C A L V I N" from assistant speech and overrides ticket first name (`Calvin Chen`).
- **False spelling parse (call-20260916-170710-b05f):** Phrase "spelling you provided" was misread as letters → ticket `Youprovided Chen`. Parser now requires true letter-by-letter tokens only (each token one letter).
- **Surname letter bleed (call-20260916-171047-e98d):** "spelled C A L V I N, Chen" captured the C of Chen → `Calvinc`. Letter-run now ends with `(?![A-Za-z])` so multi-letter surname words stop the parse.
